// Función para mostrar el mensaje de envío exitoso
function mostrarMensaje(event) {
    event.preventDefault();  // Evita que el formulario se envíe realmente (para propósitos de prueba)
    
    // Muestra el mensaje de "envío exitoso"
    alert("Envío exitoso");

    // Aquí puedes agregar más acciones si lo necesitas, como enviar el formulario a través de AJAX o redirigir.
}
